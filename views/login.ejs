<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Login</title>

  <link rel="icon" type="image/png" href="/images/favicon.png">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >

  <style>
    html {
      scroll-behavior: smooth;
    }

    html, body {
      height: 100%;
      margin: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background-color: #f8f9fa;
    }

    .container-fluid,
    .row {
      min-height: 100vh;
    }

    /* LEFT PANEL — desktop: left anchored like register */
    .left-panel {
      background: linear-gradient(135deg, #FF8A00, #FF4E00);
      color: #fff;
      display: flex;
      align-items: center;              /* vertical center */
      justify-content: flex-start;      /* push content left */
      padding: 2.5rem;
      position: relative;
      text-align: left;
    }

    .left-panel-content {
      max-width: 420px;
    }

    .brand-title {
      margin-bottom: 0.75rem;
      line-height: 1.1;
    }

    .brand-name {
      display: block;
      font-weight: 800;
      letter-spacing: -0.5px;
    }

    .brand-tagline {
      display: block;
      margin-top: 0.35rem;
      font-weight: 600;
      font-size: 1.25rem;
      opacity: 0.9;
    }

    .left-panel p {
      margin: 0;
      max-width: 360px;
    }

    /* Scroll hint */
    .scroll-hint {
      position: absolute;
      left: 0;
      right: 0;
      bottom: 16px;
      text-align: center;
      padding: 0 1rem;
    }

    .scroll-link {
      color: rgba(255, 255, 255, 0.92);
      font-weight: 600;
      font-size: 0.95rem;
      text-decoration: none;
    }

    .scroll-link:hover {
      color: #fff;
    }

    /* RIGHT PANEL */
    .login-wrapper {
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #f8f9fa;
      padding: 2rem 1rem;
    }

    .login-wrapper .card {
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.06);
      border: none;
    }

    .btn-primary {
      background-color: #FF4E00;
      border-color: #FF4E00;
      transition: all 0.2s ease-in-out;
      font-weight: 600;
    }

    .btn-primary:hover {
      background-color: #ff7a33;
      border-color: #ff7a33;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(255, 78, 0, 0.35);
    }

    /* =========================
       MOBILE: FULLSCREEN + SNAP
       ========================= */
    @media (max-width: 767.98px) {

      /* Enable snap on the page scroll */
      html, body {
        scroll-snap-type: y mandatory;
        overscroll-behavior-y: contain; /* reduces weird bounce chaining */
      }

      /* Each section is a snap point */
      .left-panel,
      .login-wrapper {
        min-height: 100vh;
        scroll-snap-align: start;
        scroll-snap-stop: always; /* helps prevent stopping between */
      }

      .left-panel {
        justify-content: center;
        text-align: center;
        padding: 3rem 2rem;
      }

      .left-panel-content {
        margin: 0 auto;
      }

      .login-wrapper {
        padding: 2.5rem 1rem;
      }

      .brand-name {
        font-size: 3rem;
      }

      .brand-tagline {
        font-size: 1.15rem;
      }
    }
  </style>
</head>

<body>

  <div class="container-fluid p-0">
    <div class="row g-0">

      <!-- LEFT PANEL -->
      <div class="col-md-6 left-panel">
        <div class="left-panel-content">
          <h1 class="brand-title">
            <span class="brand-name display-4">CampusLytics</span>
            <span class="brand-tagline">Financial Clarity for Campus Life</span>
          </h1>

          <p class="lead">
            Helps students like you keep track of where your money goes during each pay period.
            Clear breakdowns and insights help you make smarter decisions without overthinking your finances.
          </p>

          <!-- MOBILE ONLY -->
          <div class="scroll-hint d-md-none">
            <a href="#login" class="scroll-link">Scroll To Begin</a>
            <br>
            <a href="#login" class="scroll-link">↓</a>
<br><br>
          </div>
        </div>
      </div>

      <!-- RIGHT PANEL -->
      <div class="col-md-6 login-wrapper" id="login">
        <div class="card p-4" style="width: 100%; max-width: 380px;">
          <h1 class="h4 mb-3 text-center">Login</h1>

          <form action="/login" method="POST">

            <div class="mb-3">
              <label class="form-label">Username</label>
              <input type="text" class="form-control" name="username" placeholder="Enter your username" required>
            </div>

            <div class="mb-3">
              <label class="form-label">Password</label>
              <input type="password" class="form-control" name="password" placeholder="Enter your password" required>
            </div>

            <button type="submit" class="btn btn-primary w-100 mb-3">
              Log In
            </button>

            <% if (error_message) { %>
              <div class="alert alert-warning p-2 text-center">
                <%= error_message %>
              </div>
            <% } %>

            <div class="text-center mt-3">
              <p class="text-muted mb-1">Don't have an account?</p>
              <a href="/register" style="color:#FF4E00;">Sign-up</a>
            </div>

          </form>
        </div>
      </div>

    </div>
  </div>

</body>
</html>
