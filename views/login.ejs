<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Login</title>
  <link rel="icon" type="image/png" href="/images/favicon.png">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/png" href="/images/favicon.png">

  <!-- Bootstrap -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >

  <style>
    /* Make page fill the viewport */
    html, body {
      height: 100%;
      margin: 0;
    }
  
    /* Let Bootstrap handle most layout, keep body neutral */
    body {
      font-family: Arial, Helvetica, sans-serif;
      background-color: #f8f9fa;
    }
  
    /* Make the full row stretch to full height */
    .container-fluid,
    .row {
      min-height: 100vh;
    }
  
    /* LEFT HALF: gradient panel */
    .left-panel {
      background: linear-gradient(135deg, #FF8A00, #FF4E00);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 3rem;
    }
  
    .left-panel h1 {
      margin-bottom: 0.75rem;
    }
  
    .left-panel p {
      margin: 0;
      max-width: 320px;
    }
  
    /* RIGHT HALF: center login card vertically + horizontally in its column */
    .login-wrapper {
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #f8f9fa;
      padding: 2rem 1rem;
    }
  
    /* Optional: round card a bit more */
    .login-wrapper .card {
      border-radius: 12px;
    }
  
    .btn-primary {
      background-color: #FF4E00;
      border-color: #FF4E00;
      transition: all 0.2s ease-in-out;
    }

    .btn-primary:hover {
      background-color: #ff7a33; /* lighter orange */
      border-color: #ff7a33;
      transform: translateY(-3px); /* move up */
      box-shadow: 0px 4px 10px rgba(255, 78, 0, 0.3); /* optional but makes it feel cleaner */
    }

    a.text-muted:hover {
      color: #FF4E00 !important;
      }
    

  </style>
</head>

<body>

  <div class="container-fluid">
    <div class="row">

      <!-- LEFT HALF -->
      <div class="col-md-6 left-panel d-none d-md-flex">
        <div>
          <h1 class="display-4 fw-bold">College Financial Analysis/Tracker</h1>
          <p class="lead">Log in to manage your finances each payment peroid and view an analysis.</p>
        </div>
      </div>

      <!-- RIGHT HALF -->
      <div class="col-md-6 login-wrapper bg-light">
        <div class="card shadow-sm p-4" style="width: 100%; max-width: 380px;">
          <h1 class="h4 mb-3 text-center">Login</h1>

          <form action="/login" method="POST" id="loginForm">

            <div class="mb-3">
              <label class="form-label">Username</label>
              <input type="text" class="form-control" name="username" required>
            </div>

            <div class="mb-3">
              <label class="form-label">Password</label>
              <input type="password" class="form-control" name="password" required>
            </div>

            <button type="submit" class="btn btn-primary w-100 mb-3">
              Log In
            </button>

            <% if (error_message) { %>
              <div class="alert alert-warning p-2 text-center">
                <%= error_message %>
              </div>
              <% } %>
              
            <div class="text-center mt-3">
              <p class="text-muted mb-1">Don't have an account?</p>
              <a href="/register" style="color:#ff4e00;">Sign-up</a>
            </div>
  

          </form>

          
        </div>
      </div>

    </div>
  </div>

</body>
</html>
